name: flutter_ocr_app
description: A Flutter Application to Change Image to text.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  cupertino_icons: ^1.0.2
  camera: ^0.10.5+2
  google_mlkit_text_recognition:
  permission_handler: ^10.4.3
  image_picker: ^0.8.7+5
  shared_preferences:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
